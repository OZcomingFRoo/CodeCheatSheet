@{
    // - ViewStart is the file where you can prepare some data before ASP.NET will render the actual view
    // - Usually, you might want to set the Layout for all or a number of views.
    // - In this case, in here we can the define the Layout for ALL views
    //   This, of course, can be overwritten by the  
    ViewBag.Title = "ViewStart Root";
    Layout = "_Layout";
    var _ = "Root _ViewStart.cshtml will be called 2nd, i.e. after Root _ViewImports.cshtml";
    // Other Notes:
    // - Note, you can also render Html in this file
    // - Anything rendered Here will be displayed on top of the View's content
}
